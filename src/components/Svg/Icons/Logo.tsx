import React from "react";
import Svg from "../Svg";
import { SvgProps } from "../types";

const Icon: React.FC<SvgProps> = (props) => {
  return (
   

<svg version="1.2" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 172 172"   {...props}>
</svg>
  );
};

export default Icon;
